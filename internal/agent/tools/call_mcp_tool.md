Execute an MCP tool dynamically by MCP server name and tool name.
Use this for same-turn MCP chaining after discovering tools with `list_mcp_tools`, or when no direct `mcp_*` tool is already available.
