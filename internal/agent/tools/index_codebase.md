Build Sapphire's durable codebase graph for the current repository, then launch the mandatory AI semantic graph pass with multiple sub-agents so the repo also gets an AI-authored long-horizon codebase map. Use this after the user agrees to index the whole codebase, or when a large repo-wide task needs persistent repo knowledge. Avoid it for trivial or single-file work.
