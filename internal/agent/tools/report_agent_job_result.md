Worker-only tool for batch agent jobs. Report a single row's result for a
`spawn_agents_on_csv` job. Provide `job_id`, `item_id`, and a JSON `result`.
Set `stop` to true to cancel remaining items after reporting.
