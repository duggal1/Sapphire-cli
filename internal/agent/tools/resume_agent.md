Resume a previously spawned sub-agent session by agent id.
Use this when a sub-agent was created earlier and needs to be reattached or continued.
Optionally include `message` to send a new prompt immediately after resuming.
